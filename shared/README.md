# Shared contracts

This directory is reserved for versioned API contracts and other language-neutral definitions shared by the TypeScript services. Do not place Python runtime code here or couple the Python service to the frontend/backend runtime.
