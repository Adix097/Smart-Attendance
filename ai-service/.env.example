# Copy to ai-service/.env and adjust, or export these in the shell.

# HTTP server. Managed hosts that route external traffic require HOST=0.0.0.0.
HOST=127.0.0.1
PORT=8000

# Recognition defaults. Individual inference requests may override these.
AI_MODEL_NAME=buffalo_l
AI_PROVIDER=CPUExecutionProvider
AI_SAMPLING_FPS=2.0
AI_ACCEPTANCE_THRESHOLD=0.45
AI_UNKNOWN_THRESHOLD=0.35
AI_IDENTITY_MARGIN_THRESHOLD=0.05
AI_MINIMUM_OBSERVATIONS=3

# Exposes POST /v1/dev/recognition-test. Must stay false outside local testing.
AI_ENABLE_DEV_HARNESS=false
